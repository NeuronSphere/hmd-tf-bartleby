:orphan:

.. A sphinx-needs item for the needs.json export test.
..
.. This page is deliberately orphaned: needs are collected when a document is
.. read, so the item reaches needs.json without being part of the rendered
.. document, and the PDF, Word and PowerPoint tests keep the output they expect.

Needs Fixture
=============

.. req:: The transform exports its needs
    :id: BARTLEBY_TEST_REQ_001
    :status: open

    A requirement that exists only so that a build has something to export.
